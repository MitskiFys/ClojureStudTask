(ns task-one.core
  (:gen-class))

(require '[clojure.repl :refer :all])

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))

(comment
  (defn parse_string 
    [string count] 
    (
     print string
    ))
)
